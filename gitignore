# Node.js dependencies
node_modules/

# Environment variables file (contains secrets!)
.env

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pm2-*.log* # If using pm2 logs locally

# Operating System generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE/Editor directories
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Optional: Downloaded Videos Folder
# If your VIDEO_DOWNLOAD_FOLDER is *inside* your project directory (e.g., './downloads'),
# add its relative path here. It's generally better to keep downloads outside the project folder.
# downloads/ # Example if VIDEO_DOWNLOAD_FOLDER=./downloads

# Optional: PM2 files (if stored locally)
# pids/
# logs/ # Be careful if you have other log files you *do* want to track

# Optional: Cache files
.npm/
.yarn/

# Crash reports
*.crash